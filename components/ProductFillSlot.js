import { View, Text, StyleSheet } from 'react-native'
import React from 'react'

const ProductFillSlot = ({options}) => {
  return (
    <View style={styles.container}>
      
    </View>
  )
}

const styles = StyleSheet.create({

    container: {
        flex: 1,
        width: '90%'
    }

})

export default ProductFillSlot